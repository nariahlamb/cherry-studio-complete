// This is a placeholder file to fix TypeScript compilation errors
// The actual implementation is in src/renderer/src/windows/selection/action/SelectionActionApp.tsx

import React from 'react'

const SelectionAction: React.FC = () => {
  // This component is not actually used - it's just to satisfy TypeScript
  return null
}

export default SelectionAction